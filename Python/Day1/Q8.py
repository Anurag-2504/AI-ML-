'''
    Q8.  Take the radius (r) a s user input and print the area.
'''
import math
radius = float(input("Enter the radius: "))
area = math.pi * radius ** 2
print("Area of the circle with  radius", radius, "is:", area)